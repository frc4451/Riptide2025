package frc.robot.subsystems.superstructure.modes;

public enum ExitInstructions {
  NONE,
  /**
   * Move elevator into position before rotating the pivots. This is to prevent hitting the REEF.
   * For L4
   */
  ELEVATOR_BEFORE_PIVOTS,
}
